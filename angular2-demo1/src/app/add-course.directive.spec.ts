import { AddCourseDirective } from './add-course.directive';

describe('AddCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCourseDirective();
    expect(directive).toBeTruthy();
  });
});
