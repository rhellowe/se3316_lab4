import { AddAuthorDirective } from './add-author.directive';

describe('AddAuthorDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAuthorDirective();
    expect(directive).toBeTruthy();
  });
});
